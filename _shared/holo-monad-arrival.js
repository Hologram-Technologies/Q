// holo-monad-arrival.js — ceremony REMOVED (post-login monad→day-ring animation retired).
// Intentionally a no-op: kept as an empty module so any remaining static import/script resolves
// (200, not 404) while doing nothing. Canonical source no longer includes this script at all.
export {};
