// Discord Activity — public Application (Client) ID. This is a PUBLIC value; safe in the client, NO secret.
// Set it to YOUR app's ID: Discord Dev Portal → your application → "General Information" → Application ID.
// The adapter reads window.HOLO_DISCORD_CLIENT_ID to construct the SDK + complete the ready() handshake.
// discord-dev.mjs will write this for you if you pass the id as its first arg.
window.HOLO_DISCORD_CLIENT_ID = "1523651984585134222";   // Holo Messenger Activity — public Application ID
